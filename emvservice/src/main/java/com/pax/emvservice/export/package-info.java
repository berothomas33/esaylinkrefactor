/**
 * EMV service API surface.
 * <ul>
 *   <li>{@code api} — shared card/EMV interfaces
 *   <li>{@code contact} / {@code contactless} / {@code mag} / {@code manual}
 *   <li>{@code pin} / {@code version} / {@code constant} / {@code exceptions}
 * </ul>
 */
package com.pax.emvservice.export;
