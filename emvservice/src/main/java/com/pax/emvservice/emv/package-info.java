/**
 * Concrete PAX EMV services (Router-registered).
 * <ul>
 *   <li>{@code init} — module bootstrap
 *   <li>{@code contact} / {@code contactless} / {@code mag} / {@code manual}
 *   <li>{@code pin} / {@code version}
 * </ul>
 */
package com.pax.emvservice.emv;
