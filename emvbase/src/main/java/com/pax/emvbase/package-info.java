/**
 * PAX EMV base types.
 * <ul>
 *   <li>{@code constant} — tags / constants
 *   <li>{@code param} — AID / CAPK / terminal params
 *   <li>{@code process} — contact / contactless callbacks and results
 *   <li>{@code utils} — helpers
 * </ul>
 */
package com.pax.emvbase;
